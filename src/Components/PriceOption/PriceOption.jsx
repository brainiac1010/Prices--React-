
import PropTypes from "prop-types";
const PriceOption = ({option}) => {
    return (
        <div>
            
        </div>
    );
};
PriceOption.PropTypes={option:PropTypes.object.isRequired}
export default PriceOption;